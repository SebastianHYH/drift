# Postgres connection
DATABASE_URL="postgresql://drift:drift@localhost:5432/drift?schema=public"

# Shared secret protecting the cleanup cron endpoint in production.
# Call /api/cron/cleanup with header:  Authorization: Bearer <CRON_SECRET>
CRON_SECRET="change-me-in-production"
